from itertools import product
A = list(map(int, input().split())) # a ke har element ko seprate kr diya
B = list(map(int, input().split()))
result = product(A, B)  # cartesian product kr diya  a and b ke har element ka 
print(*result)  # star lagaya hai taki unpacked print ho 
